let confusedNumber: number | string = 3;

confusedNumber = "3";
console.log(`${confusedNumber} is a type of ${typeof(confusedNumber)}`);

confusedNumber = 4;
console.log(`${confusedNumber} is a type of ${typeof(confusedNumber)}`);